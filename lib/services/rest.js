'use strict';

const async = require('async');
const _ = require('lodash');

module.exports = function(mycro) {
    return {
        /**
         * Use config to define templates
         * @param  {Function} done
         */
        _initialize(done) {

        },


        /**
         * Extract query instructions from request
         * @param  {Object} req
         * @param  {Object} [options]
         * @param  {Function} cb
         */
        parse(req, options, cb) {

        }
    };
};
